<h1>{{product.name}}</h1>
<p>{{product.description}}</p>
<p>Price: ${{product.price}}</p>
<button hx-post="/cart/add" hx-include="[name=product-id]" name="product-id" value="{{product.id}}">Add to Cart</button>
