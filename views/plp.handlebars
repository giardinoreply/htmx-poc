<h1>Product List</h1>
<div id="product-list"
        class="product-grid"
        hx-get="/items?offset=0&limit=6" 
        hx-trigger="revealed" 
        hx-swap="innerHTML">
    <!-- Products will be loaded here -->
</div>